[project]
name = "marketing_cc"
version = "0.1.0"
description = "marketing-CC using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.14.4"
]

[project.scripts]
marketing_cc = "marketing_cc.main:run"
run_crew = "marketing_cc.main:run"
train = "marketing_cc.main:train"
replay = "marketing_cc.main:replay"
test = "marketing_cc.main:test"
run_with_trigger = "marketing_cc.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
